/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                hostname: "o6j7sxgc3i5u9xzy.public.blob.vercel-storage.com"
            }
        ]
    }
};

export default nextConfig;
